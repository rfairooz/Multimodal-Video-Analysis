import React from "react";
import Button from "./Button";
const Chat = () => {
  return (
    <div className=" bg-slate-900 rounded-xl p-8 w-full h-full ">
      <Button loading={false} text="Start Chat" onClick={() => {}} />
    </div>
  );
};

export default Chat;
